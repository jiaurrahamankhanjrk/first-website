/*                  // querySelector("")
let result = document.querySelectorAll(".h");
console.log(result);                                    */

/*                      // querySelectorAll("")
let a = document.querySelectorAll("#a");
console.log(a);                                         */

/*                             // change innter text by querySelector
let c = document.querySelector("#a").innerText =  `Changed by JS - document.querySelector("").innerText `;                                      */


/*                              // change html tag by querySelector
let d = document.querySelector("#l").innerHTML = `<h2>Changed by JS - document.querySelector("").innerHTML</h2>`                            */


/*                            // change innter text by querySelectorAll
let c = document.querySelectorAll(".h");  
console.log(c);                                  
for (let i = 0; i < c.length; i++) {
    let z = c[i].innerHTML = `changed`;
    console.log(z);
    
}                                                                   */